version: 0.1

phases:

  install:

    commands:
      - npm install
      - npm install -g serverless
      - npm install serverless-deployment-bucket 
      - ./s3_script.sh
              
  build:

    commands:
      - serverless deploy
