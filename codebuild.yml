version: 0.2
env:
  shell: bash

phases:

  install:

    commands:
      - npm install
      - npm install -g serverless
      - npm install serverless-deployment-bucket 
      - chmod +x s3_script.sh
      - ./s3_script.sh
              
  build:

    commands:
      - serverless deploy
